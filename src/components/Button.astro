---
interface Props{
    title : string,
    active? : boolean,
    href? : string
}

const {title,active, href } = Astro.props
---

<a href={href} target="_blank" class={`${active ? 'text-white bg-black md:inline-block' : 'text-black dark:text-white bg-white dark:bg-black'} md:text-md font-semibold text-p px-10 py-2 md:px-10 md:py-3 rounded-full`}>{title}</a>
